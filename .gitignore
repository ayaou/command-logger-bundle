# ide
.idea

# Test projects
projects

# Symfony excluded files and folders
vendor
var
composer.lock

# Docker
.docker
.data

# Quality
.php-cs-fixer.cache

# PHPUnit
phpunit.xml
.phpunit.result.cache
.phpunit.cache
.coverage
# Outillage local non versionné (voir aussi .docker/)
Makefile

# Généré par Symfony 8 au boot du kernel ("for apps only" selon le fichier lui-même)
config/reference.php
